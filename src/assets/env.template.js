(function(window) {
  window.env = window.env || {};
  window.env.BASE_URL = "${BASE_URL}";
  window.env.ACTIVATE_URL = "${ACTIVATE_URL}";
  window.env.RCTGROUP_URL = "${RCTGROUP_URL}";
  window.env.DEACTIVATE_URL = "${DEACTIVATE_URL}";
  window.env.DELETE_URL = "${DELETE_URL}";
  window.env.RESET_PASSWORD_URL = "${RESET_PASSWORD_URL}";
  window.env.UPDATED_CONSENT_URL = "${UPDATED_CONSENT_URL}";
})(this);
